services:
  blog:
    build: .
    command: sh -c "CI=true pnpm install --frozen-lockfile && pnpm start"
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - node_modules:/app/node_modules

volumes:
  node_modules:
